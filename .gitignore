.idea/
.env
*.log
out/

# Go
/bin/
*.test
*.prof
coverage.out
# A stray `go build ./cmd/x` drops the binary in the repo root, where
# `git add -A` will happily commit fifteen megabytes of it.
/cs2predictor
/backfill-team-order
/segtest
coverage.html

# Local Ansible controller and release checkout
.venv-ansible/
release/
__pycache__/

# Node (release tooling only — semantic-release)
node_modules/
